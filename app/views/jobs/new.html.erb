<h1>Create new job</h1>
<%= form_with(model: @job) do |form| %>

  <%= render partial: "/jobs/form", object: form%>

  <div class="submit-field">
    <%= form.submit "Create", class: "btn btn-success"%>
  </div>
<% end %>
